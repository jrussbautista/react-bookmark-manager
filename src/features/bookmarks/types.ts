export type Bookmark = {
  id: string;
  title: string;
  description: string;
  link: string;
  createdAt: string;
  updatedAt: string;
};

export type BookmarkFields = {
  title: string;
  description: string;
  link: string;
};
